{
	"name": "Vite Motoko SvelteKit Internet Identity Starter",
	"build": { "dockerfile": "Dockerfile" },
	"forwardPorts": [4943, 8080],
	"portsAttributes": {
		"4943": {
			"label": "dfx",
			"onAutoForward": "ignore"
		},
		"8080": {
			"label": "dev server",
			"onAutoForward": "openBrowser"
		}
	},
	"postAttachCommand": "dfx stop && dfx start --clean --background && npm run setup && npm run start",
	"customizations": {
		"vscode": {
			"extensions": ["dfinity-foundation.vscode-motoko"]
		}
	}
}
